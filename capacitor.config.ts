import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thewall.speech.app',
  appName: 'speech-brick',
  webDir: 'dist/speechBrick',
  bundledWebRuntime: false
};

export default config;
